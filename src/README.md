# src 디렉토리 구조

이 디렉토리는 애플리케이션의 핵심 소스 코드를 포함합니다.

## 📁 디렉토리 설명

### `/api`
- API 엔드포인트 및 HTTP 요청 관리
- axios, fetch 등의 API 클라이언트 설정
- API 응답 타입 정의

### `/hooks`
- 커스텀 React 훅
- 재사용 가능한 로직을 훅으로 분리
- 예: useAuth, useFetch, useForm 등

### `/utils`
- 유틸리티 함수들
- 날짜 포맷팅, 문자열 처리, 유효성 검사 등
- 순수 함수로 작성

### `/services`
- 비즈니스 로직 레이어
- 데이터 처리 및 변환
- 외부 서비스 연동

### `/store`
- 전역 상태 관리
- Context API, Zustand, Redux 등
- 상태 관련 타입 및 액션

### `/types`
- TypeScript 타입 정의
- 인터페이스, 타입 별칭
- 전역적으로 사용되는 타입

### `/config`
- 앱 설정 파일
- 환경 변수
- 상수 설정

### `/navigation`
- 네비게이션 관련 설정
- 라우트 타입 정의
- 네비게이션 헬퍼 함수

### `/features`
- 기능별 모듈 구성
- 각 기능은 독립적인 폴더로 관리
- 컴포넌트, 훅, 타입을 기능별로 그룹화
